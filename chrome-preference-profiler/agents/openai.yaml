interface:
  display_name: "Chrome Preference Profiler"
  short_description: "Build a private news-interest profile from Chrome history"
  default_prompt: "Use $chrome-preference-profiler to analyze my authorized Chrome history and refresh my private news preference profile."

policy:
  # History analysis requires explicit user authorization; never trigger implicitly.
  allow_implicit_invocation: false
